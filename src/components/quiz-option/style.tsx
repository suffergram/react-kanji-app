import styled from 'styled-components';

export const OptionContainer = styled.div`
  position: relative;
`;

export const OptionIndex = styled.p`
  position: absolute;
  left: 0.5rem;
  top: 0.25rem;
  margin: 0;
  color: #aab;
  font-size: 0.85rem;
`;
